# [个人] 零基础的逆向笔记



# Python逆向

## exe到pyc

```shell
python D:\Environment\pyinstxtractor\2025.2\pyinstxtractor.py exe.exe
```

## pyc到py

### 查看python版本

```shell
file pyc.pyc
```

### 获取python版本魔术数

```python
import importlib.util

print(importlib.util.MAGIC_NUMBER.hex())
```

### 修改pyc魔术数

### 反编译成py

#### uncompyle6

```shell
pip install uncompyle6
```

```shell
uncompyle6 pyc.pyc
```

#### decompyle3

```shell
pip install decompyle3
```

```shell
decompyle3 pyc.pyc
```

#### pycdc

```shell
git clone https://gitcode.com/GitHub_Trending/py/pycdc
```

```shell
cd pycdc
```

将`MinGW64\bin`的三个`dll`文件扔进`pycdc`静态编译

| **libstdc++-6.dll**     | **C++ 标准库**   |
| ----------------------- | ---------------- |
| **libgcc_s_seh-1.dll**  | **GCC 运行时库** |
| **libwinpthread-1.dll** | **线程库**       |

```shell
cmake -G "MinGW Makefiles" ^
-DCMAKE_BUILD_TYPE=Release ^
-DCMAKE_CXX_FLAGS="-static -static-libgcc -static-libstdc++" ^
-DCMAKE_EXE_LINKER_FLAGS="-static -static-libgcc -static-libstdc++" .
```

```shell
mingw32-make
```

得到`pycdc.exe`和`pycdas.exe`

##### 反编译

```shell
pycdc pyc.pyc
```

##### 反汇编

```shell
pycdas pyc.pyc
```
